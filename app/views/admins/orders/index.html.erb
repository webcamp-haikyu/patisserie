<div class="container">

<!-- タイトル -->
<h2 class="m-4">注文履歴一覧</h2>

	<!-- 一覧 -->
	<table class="table">
		<thead>
			<th>購入日時</th>
			<th>購入者</th>
			<th>注文個数</th>
			<th>注文ステータス</th>
		</thead>
		<% @cart_products.each do |history| %>
			<tbody>
				<td><%=  %></td>
				<td><%= history.customer.first_name %></td>
				<td><%= history.quantity %></td>
				<td><%=  %></td>
			</tbody>
		<% end %>
	</table>
</div>